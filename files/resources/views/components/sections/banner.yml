title: Banner — announcement bar
description: A thin announcement bar with a message, a link, and an optional dismiss button
category: banners
fields:
    message:
        type: text
        label: Message
        default: "Learn how to build amazing landing pages."
        required: true
    linkText:
        type: text
        label: Link text
        description: Clear to hide the link.
        default: "Read the docs"
    linkUrl:
        type: url
        label: Link URL
        default: "/#features"
    dismissible:
        type: toggle
        label: Show dismiss button
        default: "1"
