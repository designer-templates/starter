title: Hero — centered
description: Centered heading with a paragraph and two buttons
category: heroes
fields:
    heading:
        type: text
        label: Heading
        default: "The week, scheduled before Friday"
        required: true
    text:
        type: textarea
        label: Description
        rows: 3
        default: "Build next week from this week, publish it to every phone on the team, and let shift swaps sort themselves out."
    buttonText:
        type: text
        label: Primary button text
        description: Clear to hide.
        default: "Start free"
    buttonLink:
        type: url
        label: Primary button link
        default: "/pricing"
    buttonText2:
        type: text
        label: Secondary button text
        description: Clear to hide.
        default: "See how it works"
    buttonLink2:
        type: url
        label: Secondary button link
        default: "/#features"
